{
  "schemaVersion": "survival-migration-rollback-v1",
  "migrationRequired": true,
  "migration": [
    "Remove any Runtime__AllowLoopbackPeerEndpoints=true setting.",
    "For UAT only, assign stable RFC1918 IPv4 addresses to xnode containers.",
    "Set DOTNET_ENVIRONMENT=UAT explicitly; an unset environment defaults to Production and fails closed.",
    "Set matching explicit uat network identities and enable the feature.",
    "Populate exact per-recipient tuples before starting routers."
  ],
  "dataMigration": "none",
  "rollback": [
    "Disable Runtime:EnablePrivatePeerEndpoints.",
    "Remove Runtime:PrivatePeerEndpointAllowlist and Runtime:PrivatePeerNetworkIdentity.",
    "Restore publicly routable peer RPC endpoints.",
    "Revert the single local I01 commit if code rollback is required."
  ],
  "rollbackRisk": "A UAT topology that depends exclusively on RFC1918 contacts will lose inter-node onion forwarding until public endpoints or the exact policy are restored."
}
